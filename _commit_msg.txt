fix(launcher,dashboard): extensions visibles, race loadFile, lien admin

Launcher
- Sortir <aside id="extPanel"> de <main> dans les 5 themes (default,
  cosmic, dark-tactical, mystic-purple, neon-frontier) pour qu il ne
  soit plus clipe par .shell { overflow:hidden }.
- launcher-ui.js : ajouter syncExtPanelVisibility() qui n affiche le
  panneau que sur l ecran READY avec au moins une extension activee,
  hooke dans setActiveScreen() pour les transitions.
- main.js : agrandir BrowserWindow 520x260 -> 540x460 pour que le
  panneau sous la coque soit visible.
- main.js : fixer la race condition loadFile -3 (ERR_ABORTED). Le
  boot loadFile non-awaite etait supplante par celui de startSync,
  ce qui surfaceait l erreur a l utilisateur (visible sur Mac sous
  AppTranslocation). Comparaison via cle de theme normalisee au lieu
  d URL string, try/catch qui ignore -3 sur loadFile, .catch sur
  l initial loadFile.

Dashboard
- dashboard/dashboard.php : ajouter un bouton Admin dans la navbar,
  affiche uniquement si is_admin = 1. Tolere l absence de la colonne
  (migration v5 non appliquee).
